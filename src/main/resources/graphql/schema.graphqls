type Book {
    id: ID!
    title: String!
    author: String!
    year: Int!
}

type Query {
    books: [Book!]!
    bookById(id: ID!): Book
}

type Mutation {
    addBook(title: String!, author: String!, year: Int!): Book!
}
